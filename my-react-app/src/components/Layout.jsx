import SearchBar from "./SearchBar";

export default function Layout({children, query, setQuery}){
    return(
        <>
        <header>
            <nav>
                <h1>BookSearch</h1>
            </nav>
        </header>
        <main>
           
        </main>
        </>
    )
}